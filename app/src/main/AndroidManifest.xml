<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chatandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <meta-data
        android:name="com.google.firebase.messaging.default_notification_color"
        android:resource="@color/colorAccent" />

    <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="@string/default_notification_channel_id" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.chatandroid.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.chatandroid.notifications.MyFirebaseMessaging"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name="com.chatandroid.RegisterActivity" />

        <activity android:name="com.chatandroid.MainActivity" />

        <!-- Settings -->
        <activity android:name="com.chatandroid.SettingsActivity" />

        <activity android:name="com.chatandroid.ConfigurationActivity" />

        <activity android:name="com.chatandroid.chats.FindFriendsActivity" />

        <activity android:name="com.chatandroid.chats.ProfileViewActivity" />

        <activity android:name="com.chatandroid.chats.Chats" />

        <activity android:name="com.chatandroid.chats.ChatActivity" />

        <activity android:name="com.chatandroid.chats.GroupChatActivity" />

        <activity android:name="com.chatandroid.ProfileActivity" />

        <activity android:name="com.chatandroid.ProfileEditActivity" />

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

    </application>

</manifest>